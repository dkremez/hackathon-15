'use strict';

describe('DiscountsStore', function() {
  var store;

  beforeEach(function() {
    store = require('stores/DiscountsStore.js');
  });

  it('should be defined', function() {
    expect(store).toBeDefined();
  });
});
